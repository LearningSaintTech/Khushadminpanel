import DesignerInventoryForm from "../../../designer/components/inventory/DesignerInventoryForm";

/** Admin approval panel — edit designer inventory (details + images) before/during approval. */
export default function AdminDesignerInventoryEdit() {
  return <DesignerInventoryForm panel="admin" />;
}
