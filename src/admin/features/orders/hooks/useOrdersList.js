/** List data hook boundary — list state still lives in OrdersWorkspace. */
export function useOrdersList() {
  return null;
}

/** Detail data hook boundary — detail state still lives in OrdersWorkspace. */
export function useOrderDetail() {
  return null;
}

/** Status action helpers boundary. */
export function useOrderStatusActions() {
  return null;
}
