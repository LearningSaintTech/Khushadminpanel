/**
 * Thin list table boundary — rendering still owned by OrdersWorkspace.
 * Use StatusActionSelect / TrackingPanel / AfterSalesPanel for detail sections.
 */
export default function OrderListTable() {
  return null;
}
